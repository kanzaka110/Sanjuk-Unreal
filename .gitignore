# UE5 프로젝트는 별도 관리 (C:\Users\ohmil\OneDrive\문서\Unreal Projects\MonolithTest)
MonolithTest/

# Claude Code 로컬 설정 (commands, hooks, rules, agents는 Git 추적)
.claude/*
!.claude/commands/
!.claude/hooks/
!.claude/rules/
!.claude/agents/

# Secrets
.env
*.env

# Migration local config
migration/env.local
migration/*.local.json

# OS
Thumbs.db
.DS_Store
*.swp
*.pyc
__pycache__/
data/*.db

# Session-local temp artifacts (T3D dumps, rig backups)
scripts/_*
scripts/dumps/
# 단, _archive/ 는 완료된 실험 스크립트 보관용으로 커밋 허용
!scripts/_archive/
!scripts/_archive/**

# Monolith RPC diagnostic dumps (root-level .json)
/*.json

# Monolith RPC diagnostic dumps (dumps/ folder)
/dumps/

# tmp work dir
/tmp/

# Briefing 작업 폴더 임시 dump (Inspector / ANIM_REC 슬라이스 등)
Briefing/_tmp/

# ABP dump 백업 (Tuner/Inspector 작업 흔적)
Saved/

# 일회성 진단 dump (scripts/*.json)
scripts/*.json

# Monolith dump 텍스트
DevSanjuk-Unrealdump*.txt
